interface PatientRegisterFormProps {
  onDone: () => void
}

/**
 * Phase 3 / T19 placeholder. T20 replaces the body with the real form
 * (zod validation + create mutation + toast on success).
 */
export function PatientRegisterForm(_props: PatientRegisterFormProps) {
  return <p>등록 폼은 T20에서 추가됩니다.</p>
}
